import pygame
import sys
import random
import Zombie_wave_module

class Zombie_spawn:
    def __init__(self, screen, columns, type):
        self.screen = screen
        self.columns = columns
        self.type = type
    
    def draw(self):
        if self.type == 1:
            s
            # TODO regular zombie

        if self.type == 2:
            s
            # TODO regular zombie

        if self.type == 3:
            s
            # TODO regular zombie

        if self.type == 4:
            s
            # TODO regular zombie

        pass

    def move(self):
        pass
